• 
[R] 중심위지매 관한 척도 
e 결측지 저리 시 주의사항 
O my_mode함수를 결측지메도 사용 가능하도록 
수정·해보자. 
my_mode2 <- function(x) { 
x <- na.omit(x) 
} 
x_unique <- unique(x) 
x_len <- c() 
for (i in 1 :length(x_unique)) x_len[i] <- sum(x == x_unique[i]) 
x_unique[x_len == max(x_len)] 
x <- c(31, 34, 36, 33, 28, 34, 30, 34, NA, 40) 
my_mode2(x) # working 
'（-’강원LRS공유대악 
