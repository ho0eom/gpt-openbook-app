• 
[R] 중심위지매 관한 척도 
e 죄빈값 
0 기본으로 제공하는 함수가 없음 
떠빈값을 워한합수 정의 
my_mode <- function(x) { 
x_unique <- unique(x) 
x_len <- c() 
} 
for (i in 1 :length(x_unique)) x_len[i] <- sum(x == x_unique[i]) 
x_unique[x_len == max(x_len)] 
- 예제 
X <- c(1, 2, 3, 3, 5, 5) 
my_mode(x) ~。;：：;)2, 3, 3, 5, 5) I 
[1] 3 5 
'（-’강원LRS공유대악 
