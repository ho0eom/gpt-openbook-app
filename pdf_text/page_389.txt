=표준편차평균#나만의분산, 표준편차와변동계수함수my_var<-function(x, mu, N) sum((x -mu)^2)/Nmy_sd<-function(x, mu, N) sqrt(sum((x -mu)^2)/N)my_CV<-function(x, mu, N) {my_sd(x, mu, N)/mean(x)}
• 
[R] 변등성매 관한 척도 
e 분산파 표준편자 그리고 변등계수 
변동계수 
표준편자 
n이 -, 
OE 
#나만의 분산, 표준펀자와 변동계수 함수 
my_var <- function(x, mu, N) sum((x - mu)A2)/N 
my_sd <- function(x, mu, N) sqrt(sum((x - mu)A2)/N) 
my_CV <- function(x, mu, N) { 
my_sd(x, mu, N)/mean(x) 
} 
'（-’강원LRS공유대악 
